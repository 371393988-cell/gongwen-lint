# 参与贡献

欢迎通过 Issue 和 Pull Request 改进规则、测试和文档。

## 开发环境

```bash
python -m venv .venv
python -m pip install -e .
python -m unittest discover -s tests -v
```

## 提交规则建议

请说明：

1. 规则适用的文种或场景；
2. 可公开核验的依据或充分理由；
3. 至少一个应提示的合成例句；
4. 至少一个不应提示的反例；
5. 可能出现误报的边界。

请勿上传含个人信息、内部工作信息、未公开政策材料或其他敏感内容的真实文稿。
示例应当自行编写或完成匿名化处理。

## Pull Request 要求

- 保持规则确定、可解释、默认只读；
- 为新增行为编写标准库 `unittest` 测试；
- 在 README 中说明用户可见变化；
- 对可能误报的规则采用审慎提示，不自动替换原文；
- 尽量避免仅为简单功能增加第三方运行时依赖。

提交贡献即表示同意按 MIT License 许可该贡献。
