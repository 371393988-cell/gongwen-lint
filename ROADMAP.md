# 项目路线图

本路线图用于公开维护目标和验收标准。所有开发、测试和问题复现均使用公开规范、
人工编写的合成文本或程序生成的最小 DOCX，不收集、不上传真实机关文稿。

## v0.2.0：可配置规则

- 支持通过本地配置文件增加禁用词、提示词和允许词；
- 对配置文件进行结构校验，并给出可定位的错误信息；
- 增加配置合并、重复规则和编码异常测试；
- 保持默认离线、只读、无网络依赖。

验收标准：Windows 和 Linux、Python 3.10 与 3.12 的测试全部通过；无配置时保持
v0.1.0 的默认行为。

## v0.3.0：报告与集成

- 增加 SARIF 输出，便于在代码审查平台统一查看结果；
- 为 JSON 报告发布明确的结构版本；
- 支持仅输出指定严重程度或规则类别；
- 增加不泄露输入正文的摘要报告模式。

验收标准：示例输出可以通过对应格式校验；隐私摘要不包含命中原文和本地绝对路径。

## v0.4.0：DOCX 深化检查

- 完善标题、结构层次和附件编排检查；
- 增加表格、页眉页脚及分节文档的边界测试；
- 为 ZIP/XML 解析增加资源限制和恶意输入回归测试；
- 继续明确机械检查与人工视觉复核的边界。

验收标准：只使用合成 DOCX 覆盖正常、损坏及恶意输入；发现问题时不修改源文件。

## 暂不纳入

- 上传文稿到云端进行校对；
- 自动替换或覆盖用户原文件；
- 收集真实机关公文建立公开语料库；
- 对政治审核、合法性审核、保密审查作自动合格判断。

欢迎通过 GitHub Issue 讨论优先级。提交建议时，请勿粘贴真实敏感文稿。
