# 安全与隐私

本工具默认在本地进行只读检查，不包含网络请求或文件上传功能。

JSON 和 Markdown 报告会记录被检查文件的路径及命中片段。公开报告前，应当检查
其中是否含个人信息、内部路径、未公开事项或其他敏感内容。

请使用 GitHub 仓库的 Private vulnerability reporting 功能报告安全问题，不要在
公开 Issue 中粘贴真实敏感文稿。复现材料应使用自行编写的合成文本或最小 DOCX。

## DOCX 输入边界

DOCX 是 ZIP/XML 容器。工具在解析 XML 前检查归档大小、成员数量、单成员大小、
总解压大小、压缩比、路径穿越、重复成员、加密成员和符号链接，并拒绝 XML DTD
与实体声明。默认限制旨在阻断常见资源消耗和归档路径攻击；超过限制的文件会以
错误退出，不会解压到磁盘，也不会修改源文件。

当前默认值：归档 100 MiB、2048 个成员、单成员 64 MiB、总解压 256 MiB、
压缩比 1000:1、单个 XML 32 MiB。若合法文档超过限制，请先在隔离环境中核实其
来源和结构，再提交不含真实内容的最小复现说明。
